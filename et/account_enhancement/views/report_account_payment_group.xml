<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_payment_group.action_report_payment_group" model="ir.actions.report">
        <field name="print_report_name">
            (object.partner_type == 'supplier' and 'OP' or 'Recibo') + ' ' + (object.partner_id.name or '') + ' ' + (object.document_number or 'Borrador')
        </field>
    </record>
</odoo>

