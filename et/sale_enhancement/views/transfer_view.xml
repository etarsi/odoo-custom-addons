<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale_order_form_inherit_wms_transfer_button" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.wms.transfer.button</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <!-- En el button box de Odoo -->
            <xpath expr="//sheet/div[@name='button_box']" position="inside">

                <button type="object"
                        name="action_open_wms_transfer"
                        class="oe_stat_button"
                        icon="fa-exchange"
                        attrs="{'invisible': [('transfer_id', '=', False)]}">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Transferencia</span>
                    </div>
                </button>

            </xpath>
        </field>
    </record>

</odoo>