<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <!-- Extiende los botones del ListView -->
    <t t-extend="ListView.buttons">
        <!-- Insertar nuestro botón luego de "Crear" y que solo se vea en hr.attendance -->
        <t t-jquery="button.o_list_button_add" t-operation="after">
            <!-- Mostramos el botón SOLO en hr.attendance (y opcionalmente si viene el flag de contexto) -->
            <t t-if="widget.modelName === 'hr.attendance'">
                <button type="button" class="btn btn-primary open_wizard_button">
                    <i class="fa fa-plus mr-1"/>
                    Registrar Asistencia
                </button>
            </t>
        </t>
        <!-- Insertar nuestro botón luego de "Crear" y que solo se vea en hr.attendance -->
        <t t-jquery="button.o_list_button_add" t-operation="after">
            <!-- Mostramos el botón SOLO en hr.attendance (y opcionalmente si viene el flag de contexto) -->
            <t t-if="widget.modelName === 'general.ledger.report.wizard'">
                <button type="button" class="btn btn-primary open_wizard_button">
                    <i class="fa fa-plus mr-1"/>
                     Exportar a Excel
                </button>
            </t>
        </t>

        
    </t>
</templates>