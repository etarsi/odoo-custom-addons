<odoo>
    <record id="action_export_payroll_excel" model="ir.actions.server">
        <field name="name">Exportar a Excel</field>
        <!-- Modelo destino -->
        <field name="model_id" ref="model_hr_payroll_salary"/>
        <!-- Llamamos a tu método Python -->
        <field name="state">code</field>
        <field name="code">action = records.action_generar_excel()</field>
        <!-- Enlazar a Action menu del modelo -->
        <field name="binding_model_id" ref="model_hr_payroll_salary"/>
        <field name="binding_type">action</field>
    </record>

    <!-- ACTION PARA HR.EMPLOYEE -->
    <record id="action_horario_laboral_defecto_all" model="ir.actions.server">
        <field name="name">Asignar Horarios Laborales por Defecto</field>
        <field name="model_id" ref="model_hr_employee"/>
        <field name="state">code</field>
        <field name="code">records.action_horario_laboral_defecto_all()</field>
        <field name="binding_model_id" ref="model_hr_employee"/>
        <field name="binding_type">action</field>
    </record>

    <record id="action_horario_laboral_administrativo" model="ir.actions.server">
        <field name="name">Asignar Horarios Laborales Administrativo</field>
        <field name="model_id" ref="model_hr_employee"/>
        <field name="state">code</field>
        <field name="code">records.action_horario_laboral_administrativo()</field>
        <field name="binding_model_id" ref="model_hr_employee"/>
        <field name="binding_type">action</field>
    </record>

    <record id="action_horario_laboral_operativo" model="ir.actions.server">
        <field name="name">Asignar Horarios Laborales Operativo</field>
        <field name="model_id" ref="model_hr_employee"/>
        <field name="state">code</field>
        <field name="code">records.action_horario_laboral_operativo()</field>
        <field name="binding_model_id" ref="model_hr_employee"/>
        <field name="binding_type">action</field>
    </record>
</odoo>
